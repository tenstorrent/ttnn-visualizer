{
    "version": "0.0.1",
    "configurations": [
        {
            "name": "backend",
            "runtimeExecutable": "bash",
            "runtimeArgs": [
                "-c",
                "DEBUG=true exec uv run python -m ttnn_visualizer.app"
            ],
            "port": 8000
        },
        {
            "name": "frontend",
            "runtimeExecutable": "bash",
            "runtimeArgs": [
                "-c",
                "export NVM_DIR=\"${NVM_DIR:-$HOME/.nvm}\"; [ -s \"$NVM_DIR/nvm.sh\" ] && . \"$NVM_DIR/nvm.sh\" && nvm use >/dev/null 2>&1; exec pnpm run dev"
            ],
            "port": 5173
        }
    ]
}
